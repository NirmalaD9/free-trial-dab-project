resources:
  pipelines:
    auto_dlt_pipeline:
      name: auto_dlt_pipeline_${bundle.target}

      continuous: true
      serverless: true

      catalog: workspace
      target: ${bundle.target}

      libraries:
        - file:
            path: ../notebooks/dlt_pipeline.py